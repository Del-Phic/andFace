# Copy to .env for Docker Compose. Replace both placeholders before starting.
DB_USERNAME=andface
DB_PASSWORD=replace-with-a-random-database-password
JWT_SECRET=replace-with-base64-of-at-least-32-random-bytes
# Local PostgreSQL, outside Docker:
DB_URL=jdbc:postgresql://localhost:5432/andface
